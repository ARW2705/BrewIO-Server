const defaultIdRegex = new RegExp(/^[\d]{13,23}$/);

// Default ids generated by client is unix timestamp in milliseconds
// Mongo generated ids are 24 chars long
module.exports = id => {
  return defaultIdRegex.test(id);
}
